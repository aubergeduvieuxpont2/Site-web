{
  "name": "@site-web/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env-file ../../.dev.env",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "db:migrate": "node scripts/migrate.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "@neondatabase/serverless": "^0.10.4",
    "hono": "^4.7.10",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250607.0",
    "vitest": "^2.0.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.19.1"
  }
}
